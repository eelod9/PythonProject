myArray = ['hello', 'kitty', 'dog', 'happy', 'poppy'];
console.log("Hello")
const newarray = myArray.map((nu) => { 
    {val: nu}
});

console.log(newarray[0]);

var mydict ={
    val : "hello"
};

const [fn,ln] = ["ella", "got"];
//console.log(fn)

const {name: username, age}={
    name: "Ella",
    age: 31
};

console.log(username + " " + age)